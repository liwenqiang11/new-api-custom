# Runtime image using the current prebuilt New API binary.
# Credentials and database state are supplied at runtime through env/volumes.
FROM calciumion/new-api:latest

COPY new-api-full /new-api
RUN chmod +x /new-api

EXPOSE 3000
WORKDIR /data
ENTRYPOINT ["/new-api"]
